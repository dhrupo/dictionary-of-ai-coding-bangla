<!-- কী: bangla-commit স্কিলের নমুনা-ফাইল — ভালো/খারাপ কমিট মেসেজের উদাহরণ | কোথায় শেখানো: hands-on/04 (SKILL.md), dictionary/06 (প্রগ্রেসিভ ডিসক্লোজার) | ব্যবহার: SKILL.md-র সঙ্গেই কপি হয়; এজেন্ট দরকার পড়লে তবেই এটা খোলে -->

# কমিট মেসেজের নমুনা

> 💡 **এই ফাইলটা আলাদা কেন?** SKILL.md ছোট থাকে (নিয়মটুকুই), আর ভারী উদাহরণগুলো এখানে —
> এজেন্ট দরকার পড়লে তবেই খোলে। এটাই *দুই-স্তরের প্রগ্রেসিভ ডিসক্লোজার*:
> স্কিল নিজেই দরকারে খোলে (স্তর ১), স্কিলের ভেতরের নমুনা আরও দরকারে (স্তর ২)।
> (এই `references/` ভাগ-করা ধরনটা [nkb-bd/ff-team-skills](https://github.com/nkb-bd/ff-team-skills) লাইব্রেরির প্যাটার্ন থেকে নেওয়া।)

## ✅ ভালো

```
লগইন ফর্মে খালি পাসওয়ার্ডের বাগ ঠিক করা হলো

validateUser() খালি স্ট্রিংয়েও true ফেরত দিচ্ছিল।
এখন আগে length চেক হয়, তারপর যাচাই।
```

```
যোগাযোগ ফর্মে ইমেইল ভ্যালিডেশন যোগ করা হলো
```

## ❌ খারাপ — আর কেন

```
fix
```
*(কী fix? কোথায়? পরে কেউ ইতিহাস ঘেঁটে কিছুই বুঝবে না।)*

```
লগইনের বাগটা ঠিক করলাম আর CSSও একটু গুছালাম আর README-ও আপডেট করলাম
```
*(তিনটে আলাদা কাজ এক কমিটে — ভাগ করো। আর ৫০ অক্ষর পেরিয়ে গেছে।)*

```
Fixed the empty password bug in validateUser
```
*(নিয়ম ভাঙা হয়েছে — এই প্রজেক্টে মেসেজ বাংলায়; technical শব্দ ইংরেজি থাকতে পারে, পুরো লাইন নয়।)*
