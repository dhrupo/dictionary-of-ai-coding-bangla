name: লিংক যাচাই (link check)

on:
  pull_request:
  push:
    branches: [main]

jobs:
  check-links:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: সব markdown লিংক ও anchor যাচাই
        run: python3 scripts/check-links.py
